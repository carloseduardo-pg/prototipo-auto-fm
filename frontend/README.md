# Frontend

SPA React + Vite + TypeScript. Tokens e layout copiados de `referencia-ui/`.

```bash
npm run dev --prefix frontend
```

UI em http://localhost:5190. A API precisa estar em `:3000`.

Login seed (não pré-preenchido na tela): `operador@fm.local` / `fm123456`.
