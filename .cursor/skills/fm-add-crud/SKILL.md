---
name: fm-add-crud
description: >-
  Adds a Nest + React resource in Distac style for FM domain (shipments, users).
  Use when creating a new CRUD or list+form screen.
---

# FM — novo recurso

1. Spec em `docs/projeto/aplicacoes/` + módulo em `docs/projeto/modulos/`
2. Prisma inglês → Controller/Service Nest → página React pt-BR
3. Paginação; `Icon`; tokens FM
4. Espelhar `referencia-ui/` se a tela já existir no mockup
5. Atualizar `STATUS_PROTOTIPO.md` e `FLUXO-APLICACAO.md`
