# Hub Início

Rota: `/`. Mockup: `referencia-ui/inicio.html`. API: `GET /api/dashboard/summary`.

Métricas do mockup: prontas para emitir; aguardando fonte; com divergência; emitidas hoje.

Copy: saudação + resumo da última coleta (horário, cliente núcleo M. Dias, quantas remessas, quantas prontas). CTA “Ir para Remessas”.
