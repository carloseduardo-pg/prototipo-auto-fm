# Banco de dados

Scripts em [`../../database/`](../../../database/). Prisma em `backend/prisma/`. Database local `fm_auto`.

Triggers: `audit_log` + `fn_shipment_issue_guard` (não marcar emitido sem registro de emissão). Ver `database/info/triggers.md`.
